---
name: __PLUGIN_ID__
description: TODO — one line, specific enough to decide "does this apply right now"
metadata:
  type: skill
  tier: A
---

# __DISPLAY_NAME__

<!--
  Generated by the Plugin-Builder (ADR-0253) from the `skill_plugin` template.

  Tier A (ADR-0156): prompt-only, Markdown. No executable code runs from this
  file — the skill is instructions an engine reads, not a program it runs.
  That is what makes Tier A always-unlimited on every license tier: there is
  nothing here to sandbox.

  Keep this tight and instruction-shaped. A skill is prompt-injected into a
  live turn; padding costs context on every invocation, not just this one.
-->

## When to use this

TODO — the trigger condition. Be specific: "the user asks to X" beats "when
relevant".

## What to do

TODO — the actual instructions. Steps, constraints, examples as needed.

## What NOT to do

TODO — the boundary. What looks like this task but isn't; where to stop and
hand back to the user instead of guessing.
