#!/bin/sh
# Legacy entry point — kept so existing docs and muscle memory keep working.
# The implementation is uninstall.sh (backup → stop → unregister → remove →
# verify, Docker export per ADR-0868). `--force` / `--purge` mean `--yes`.
exec bash "$(dirname "$0")/uninstall.sh" "$@"
